import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import { Breadcrumb, DemoNotice, MetaLine, SectionHeading } from "../components";
import { projects } from "../data";

export default function ProjectsPage() { return <main><DemoNotice /><div className="page-wrap inner-page"><Breadcrumb current="Projects" /><div className="page-intro"><MetaLine>THE ARCHIVE / PROJECTS IN MOTION</MetaLine><h1>Where a connection<br /><em>becomes a project.</em></h1><p>Projects hold the in-between: why people met, what they are trying, and what remains unresolved.</p></div><div className="project-list">{projects.map((project, index) => <Link className="project-row" href={`/projects/${project.slug}`} key={project.slug}><span className="project-number">0{index + 1}</span><div className="project-mark"><span>{project.artists[0].slice(0, 2)}</span><span>{project.artists[1].slice(0, 2)}</span>{project.artists[2] && <span>{project.artists[2].slice(0, 2)}</span>}</div><div className="project-main"><MetaLine>{project.meta}</MetaLine><h2>{project.title}</h2><p>{project.description}</p><div className="project-participants">{project.artists.map(name => <span key={name}>{name}</span>)}</div></div><div className="project-status">{project.status}<ArrowUpRight size={18} /></div></Link>)}</div><section className="project-manifesto"><SectionHeading title="Why we meet" /><div><p>Some projects begin with a clear proposal. Others begin with a person staying in the room a little longer.</p><Link href="/connections" className="text-link">See the relationship archive <ArrowUpRight size={14} /></Link></div></section></div></main>; }
