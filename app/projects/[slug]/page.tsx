import Link from "next/link";
import { ArrowLeft, ArrowUpRight } from "lucide-react";
import { ArtImage, Breadcrumb, DemoNotice, MetaLine, SectionHeading } from "../../components";
import { projects } from "../../data";

export default async function ProjectDetail({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const project = projects.find(p => p.slug === slug) ?? projects[0]; return <main><DemoNotice /><div className="page-wrap inner-page project-detail"><Breadcrumb current={project.title} /><Link className="back-link" href="/projects"><ArrowLeft size={15} /> All projects</Link><div className="detail-hero"><div><MetaLine>{project.meta}</MetaLine><h1>{project.title}</h1><p>{project.description}</p></div><div className="detail-stamp"><span>u.a.u</span><small>Project archive<br />{project.status}</small></div></div><ArtImage className="detail-image" label={`${project.title} project documentation`} /><section className="project-content"><div><MetaLine>01 / WHY WE MET</MetaLine><h2>A room can stay open<br /><em>after everyone leaves.</em></h2></div><div><p>After UNFRAME Salon 04, Seo Yujin, Han Mira, and Yoon Doyun kept returning to a question: what kinds of connection can hold without asking anyone to become the same?</p><p>They are sharing references, unfinished fragments, and small invitations. The project is still in process — which is exactly where it belongs for now.</p></div></section><section className="timeline-section"><SectionHeading title="The thread so far" /><div className="timeline"><div><span>03.2026</span><strong>UNFRAME Salon 04</strong><p>First encounter in Seoul.</p></div><div><span>05.2026</span><strong>Studio conversation</strong><p>Three practices, one table.</p></div><div><span>08.2026</span><strong>u.a.u Project</strong><p>Sharing process in public.</p></div><div><span>Next</span><strong>Still becoming</strong><p>Follow the thread.</p></div></div></section><section className="project-cta"><h2>Stay close to<br /><em>the next part.</em></h2><Link className="button button-blue" href="/connections">Explore their connections <ArrowUpRight size={16} /></Link></section></div></main>; }
