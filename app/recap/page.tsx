import Link from "next/link";
import { ArrowUpRight, CalendarDays, MapPin, Sparkles } from "lucide-react";
import { Breadcrumb, DemoNotice, MetaLine, SectionHeading } from "../components";
import { annualRecap } from "../data";

export default function RecapPage() {
  return <main><DemoNotice /><div className="page-wrap inner-page recap-page"><Breadcrumb current="Connected 2027" /><section className="recap-hero"><div><MetaLine>u.a.u ANNUAL CONNECTION RECAP</MetaLine><h1>CONNECTED<br /><em>2027</em></h1><p>{annualRecap.intro}</p><div className="recap-event-meta"><span><CalendarDays size={15} /> Annual record</span><span><MapPin size={15} /> Seoul · Berlin · Busan</span></div></div><div className="recap-orbit" aria-hidden="true"><span>u.a.u</span><i /><i /><i /><b>2027</b></div></section><section className="recap-stat-board"><div className="recap-stat-heading"><MetaLine>THE YEAR IN NUMBERS</MetaLine><p>Not a leaderboard. A record of what moved between people.</p></div>{annualRecap.stats.map(([value, label]) => <div className="recap-stat" key={label}><strong>{value}</strong><span>{label}</span></div>)}</section><section className="recap-story"><SectionHeading title="The thread this year" /><div><p>In 2027, the unit grew less by gathering names and more by making room for the next conversation. A studio visit became a shared reference. A show became a project. A city became a point on the map.</p><p>These are the connections we are carrying forward.</p><Link className="text-link" href="/connections">Open the connection map <ArrowUpRight size={14} /></Link></div></section><section className="recap-awards"><SectionHeading title="Connection awards" /><div className="award-list">{annualRecap.awards.map(([award, winner, description], index) => <div className="award-row" key={award}><span>0{index + 1}</span><div><MetaLine>{award}</MetaLine><h3>{winner}</h3><p>{description}</p></div><Sparkles size={16} /></div>)}</div></section><section className="recap-gathering"><MetaLine>CONNECTED 2027 / ANNUAL RECAP &amp; GATHERING</MetaLine><h2>Come back<br /><em>for the next connection.</em></h2><Link className="button button-blue" href="/join">Find your way in <ArrowUpRight size={16} /></Link></section></div></main>;
}
